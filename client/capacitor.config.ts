import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.ofcpokerclient',
  appName: 'OFC Poker',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
